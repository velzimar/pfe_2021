{% extends 'base.html.twig' %}

{% block title %}Service{% endblock %}
{% block  breadcrumb %}
    <ul class="breadcrumb">
        <li class="breadcrumb-item">
            <a href="#">Home</a>
        </li>
        <li class="breadcrumb-item">
            <a href="{{ path('myServices') }}">Mes services</a>
        </li>
        <li class="breadcrumb-item">
            <a href="#">Détails</a>
        </li>
    </ul>
{% endblock %}

{% block QuickLink1 %}
    {{ include('service/_myQuickLinks.html.twig') }}
    <a class="btn btn-white btn-sm" href="{{ path('myServices_edit', {'id': service.id}) }}">
        <i class="os-icon os-icon-delivery-box-2"></i>
        <span>Modifier ce service</span>
    </a>
    {{ include('service/myDelete_form.html.twig') }}
{% endblock %}
{% block body %}
    <div class="col-sm-12">
        <div class="element-wrapper">
            <h6 class="element-header">
                Mes services
            </h6>
            <div class="element-box">
                <h5 class="form-header">
                    Détails du service
                </h5>
                <div class="form-desc">
                    <table class="table">
                        <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ service.id }}</td>
                        </tr>
                        <tr>
                            <th>Nom</th>
                            <td>{{ service.nom }}</td>
                        </tr>
                        <tr>
                            <th>Description</th>
                            <td>{{ service.description }}</td>
                        </tr>
                        <tr>
                            <th>Prix</th>
                            <td>{{ service.prix }}</td>
                        </tr>
                        <tr>
                            <th>Qtt</th>
                            <td>{{ service.qtt }}</td>
                        </tr>
                        <tr>
                            <th>Catégorie</th>
                            <td>
                                <a class="product-remove-btn"
                                   href="{{ path('myServiceCategories_show', {'id':service.category.id}) }}">
                                    {{ service.category }}
                                </a>
                            </td>
                        </tr>

                        {% if service.filename %}
                            <tr>
                                <th>Image</th>
                                <td>
                                    <img alt="" src="{{ vich_uploader_asset(service, 'imageFile') }}">
                                </td>
                            </tr>
                        {% endif %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
